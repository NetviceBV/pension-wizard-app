

## Add Vercel Analytics

Install the `@vercel/analytics` package and add the `<Analytics />` component to the app layout.

### Changes

1. **Install package**: `@vercel/analytics`

2. **`src/App.tsx`**: Import `Analytics` from `@vercel/analytics/react` and add `<Analytics />` inside the component tree (next to the Toaster components).




## Plan

Update the label in `ResultBlock` in `src/components/Calculator.tsx`:

Change `"Pensioengevend inkomen per jaar"` → `"Pensioengevend inkomen per jaar (op fulltime basis)"`

Single line change around line 156.

